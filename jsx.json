{
  "BUTTON-ONCLICK": {
    "prefix": "button-onClick",
    "body": [
      "      <button onClick={() => myFunction(\"hi\")}>Click Me</button>"
    ],
    "description": "BUTTON-ONCLICK"
  },
  "MAP_REACT": {
    "prefix": "map-react",
    "body": [
      "      {",
      "        <div>",
      "          {${1:arr}.map((item, index) => (",
      "            <div key={item.somethingInItem}>",
      "              <h1>{item}</h1>",
      "            </div>",
      "          ))}",
      "        </div>",
      "      }"
    ],
    "description": "MAP_REACT"
  },
  "BUTTON-REDUX": {
    "prefix": "button-redux",
    "body": [
      "    name-{name}",
      "    <button onClick={() => dispatch(nameFetch(3))}>-</button>"
    ],
    "description": "BUTTON-REDUX"
  }
}
